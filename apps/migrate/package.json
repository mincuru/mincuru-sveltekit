{
  "name": "@repo/migrate",
  "version": "1.0.0",
  "description": "Database migration function on AWS Lambda with prisma client",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.496.0",
    "@repo/database": "*",
    "@repo/typescript-config": "*",
    "typescript": "^5.3.3"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131"
  }
}
